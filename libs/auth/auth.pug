html
  title Добро пожаловать!

  h2 Для доступа введите, пожалуйста, логин <code>admin</code> и пароль <code>admin</code>.

  p Так как данные могут быть модифицированы посетителями, мы не несем ответственности за текущее содержимое. База данных сбрасывается каждые 15 минут.

  form(action="/login" method="post")
    p Логин:
    input(type="text" name="login" placeholder="admin")

    p Пароль:
    input(type="text" name="password")
